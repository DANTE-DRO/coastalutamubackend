services:
  - type: web
    name: utamu-agency-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: ADMIN_PASSWORD
        sync: false
      - key: SESSION_SECRET
        generateValue: true